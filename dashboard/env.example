# Dashboard Environment Variables

# Environment
NODE_ENV=production

# Backend API URL (update after backend is deployed)
NUXT_PUBLIC_API_BASE_URL=https://your-backend-url.railway.app/api

# Deployment Mode
# Options: "cloud" | "enterprise" | "self-hosted"
# Must match backend DEPLOYMENT_MODE
# - cloud: Shows billing UI, upgrade prompts, Stripe integration
# - enterprise: Shows premium features, no billing UI
# - self-hosted: Shows core features only, enterprise upsell (default)
NUXT_PUBLIC_DEPLOYMENT_MODE=self-hosted

# Stripe (only required for "cloud" mode)
NUXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Upsell URLs (customize where upgrade links point to)
NUXT_PUBLIC_ENTERPRISE_URL=https://airatelimit.com/enterprise
NUXT_PUBLIC_CLOUD_SIGNUP_URL=https://airatelimit.com/signup

