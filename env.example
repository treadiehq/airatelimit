# Backend Environment Variables

# Environment
NODE_ENV=production

# Server
PORT=3000

# Database (Railway will provide this)
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=7d

# Encryption key for API keys at rest (generate with: openssl rand -hex 32)
# If not set, encryption is disabled (development mode)
ENCRYPTION_KEY=your-32-byte-hex-encryption-key

# CORS (Your dashboard URL)
CORS_ORIGIN=https://your-dashboard.railway.app

# Email (Resend)
RESEND_API_KEY=re_your_resend_api_key
EMAIL_FROM=noreply@yourdomain.com

# Optional: Global admin API key
GLOBAL_ADMIN_KEY=your-admin-key-for-management
